[project]
name = "pubmed-mcp-server"
version = "1.0.0"
description = "MCP server for PubMed/NCBI E-utilities API — search, fetch, and verify citations"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0.0",
    "httpx>=0.27",
]

[project.scripts]
pubmed-mcp = "server:main"
